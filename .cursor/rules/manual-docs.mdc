---
description: docs/manual 与 07 手册的编写规范（枚举说明、操作流程、可复制命令）
globs: docs/**/*.md
alwaysApply: false
---

# 用户手册编写规范

## 目标读者

不懂系统设计的验收人/家人朋友：只看 `docs/manual/` + 复制命令即可完成操作。

## 枚举 / 选项必须写清

凡 CLI flag、payload 字段、status 等**有限选项**，禁止只写 `a|b|c`，必须附表：

| 选项 | 中文名 | 什么时候用 | 下一步 |
|---|---|---|---|

- 跨命令复用的枚举 → 抽到 `docs/manual/ref-*.md`，H* 手册只保留摘要 + 链接
- cobra `--help` 里的说明须与手册一致

## 接口页必含（见 07 §五 + `_template-功能接口.md`）

1. 一句话（解决什么用户问题）
2. 前置条件（环境变量、依赖状态）
3. 签名 + **参数表**（必填、默认值、说明）
4. 成功/失败 stdout 各 ≥1 条完整样例
5. 手动验证 numbered list
6. 关联 ref-* 与操作流程

## 操作流程

- 端到端路径、分叉、状态机 → `docs/manual/01-操作流程总览.md`
- 单命令细节 → 对应 H* 手册
- 07 总纲索引两者，禁止只在 H* 里隐含流程

## 禁止

- 只有签名没有参数说明
- 只有摘要没有可复制命令
- 新增字段无 ref 或 example JSON
- 改 CLI 行为不更新手册与 07 索引
